Master Prompt — Quiz (LISM Lesson)
Fill in the bracketed fields, then paste this whole prompt into ChatGPT, Claude, Gemini or any AI tool.
Save the HTML it returns as a .html file and upload it to LISM Lesson.

Fill in: [TOPIC NAME] · [SUBJECT] · [GRADE] · [WEEK X]
Then set your question mix in section 4 — how many multiple choice, true/false,
fill-in-the-blank and so on. The totals follow from your numbers.

Build a single self-contained interactive HTML quiz for [TOPIC NAME] — [SUBJECT], Grade [GRADE], Week [WEEK X].
One HTML file. Inline CSS and JS only, no external assets, no build step, no markdown fences in your reply.
It must open and work correctly by double-clicking the file, offline, with no LISM present — and work as a
teacher-paced activity when uploaded to LISM.

A scored quiz mixing question formats, checking understanding across the whole topic.


1. SUBJECT PROFILE (set this first — it drives layout, language and validation)

State this config object at the top of the file and make every part of the activity obey it. Infer
sensible values from [SUBJECT] and [TOPIC NAME]:

const PROFILE = {
  subject:'[SUBJECT]', grade:'[GRADE]', week:'[WEEK X]', topic:'[TOPIC NAME]',
  lang:'en',      // 'en' | 'ar' | 'fr' | 'es' | 'ur' | 'hi' — language of instruction
  dir:'ltr',      // 'rtl' for Arabic and Urdu — mirrors the whole layout
  math:false,     // true → maths rendering + symbol palette
  chem:false,     // true → chemical formula handling
  code:false,     // true → code editor behaviour
  images:true     // true → allow image attachment where it makes sense
};

2. LANGUAGE, SCRIPT AND RTL — ALL SUBJECTS INCLUDING ARABIC (mandatory)

This activity must work in any subject and any language the school teaches, Arabic included.

- Set <html lang> and dir from PROFILE. When dir:'rtl', mirror the ENTIRE layout — headers, progress
  indicators, buttons, cards, tables and badges all flow right-to-left. Next moves left, Previous moves
  right visually, but arrow keys keep their logical meaning.
- Keep numbers, formulae and code in dir:'ltr' islands inside RTL text so they do not reverse.
- Arabic normalisation before comparing any answer: strip tashkeel/diacritics, remove tatweel (ـ),
  unify alef (أ إ آ ٱ → ا), ى → ي, ة → ه, unify hamza forms, and convert Arabic-Indic digits
  (٠١٢٣٤٥٦٧٨٩) to Western digits. "الْمَاء" and "الماء" must both be accepted.
- Script-aware counting: count words for Arabic, Urdu and Hindi properly; for Chinese/Japanese use
  characters ÷ 2. Never use a Latin-only regex to judge length.
- Never apply an English vowel-based gibberish test to non-Latin script. For those, detect only
  repeated-character runs and single-character spam.
- French/Spanish/Urdu: accent-insensitive comparison for short answers, correct accents kept in the
  model answers.
- All on-screen instructions, buttons and feedback appear in PROFILE.lang. For a bilingual class, show
  the term and its translation together.
- Use a font stack that renders Arabic correctly and set a comfortable line-height for Arabic script.

3. ACCESSIBILITY AND DEVICE SUPPORT

Readable on a smart board and on a phone. Font sizes with clamp(), single column below 900px, touch
targets at least 44px, inputs at least 16px so iOS does not zoom. Full keyboard operation, visible
focus rings, and colour never used as the only signal. Nothing overlaps at any width.

4. STRUCTURE

YOU CHOOSE THE MIX AND THE LENGTH. Write a number in each bracket below -- any number you want.
Leave a format blank or 0 and it will not appear. These are not suggestions or limits; build exactly
the numbers written:

  Multiple choice      [  ]   4 options, one correct; every wrong option a named misconception
  True / false         [  ]   each with a one-line justification box
  Fill in the blank    [  ]   short answer; accept a pipe-separated list of spellings, and
                              mark it by the MARKING A TYPED ANSWER rules below
  Short answer         [  ]   a phrase marked against an answer key of accepted wordings
  Numeric              [  ]   value, plus units where the subject uses them
  Ordering / matching  [  ]
  Open-ended           [  ]   a written answer the TEACHER marks -- see AUTO-MARKING below

Use only the formats whose count is above zero, and leave out every format set to 0. Total questions
= the sum of those counts. Total marks = the same number, one mark per question. Order them so
demand rises from recall to application to analysis.

There is no fixed ceiling, and no format is capped: 30 multiple choice on their own is a valid quiz,
so is 30 open-ended, so is any combination. Whatever the numbers, everything must still scale --
question numbering, the layout at 1366x768 and on a 390px phone, the manifest, the running score and
the results section. If a count is large, keep each question compact rather than dropping any.

PACING -- pick one and build only that one:
  [ONE SECTION]   every question in a single section, students working down the page at their own
                  pace. Use this for a test, and for anything over about 10 questions.
  [PER QUESTION]  one section per question, released one at a time by the teacher. Good for a
                  whole-class quiz show. Do not use it beyond 10 questions: the teacher has to press
                  "Start next section" once for every question.

MARKING A TYPED ANSWER -- a correct answer must never be told it is wrong:
  Normalise both sides first: lowercase, trim, collapse repeated spaces, strip accents.
  Then, before marking anything wrong, three more passes:
    1. Compare again with spaces, hyphens and full stops removed from both sides, so
       power point = PowerPoint = power-point, and web site = website. A student who spaces
       a compound word differently has not made a mistake.
    2. Do not demand a word the question has already printed. Where the sentence shows part
       of the term -- "it is called the ____ Test" -- the key "Turing Test" must also accept
       "Turing" on its own. Strip from the key the words printed immediately before and after
       the blank, accept what remains, and accept the full phrase too.
    3. Ignore a leading article (a, an, the) and any trailing punctuation.
  Only when all three still fail is the answer wrong. When in doubt, award the mark and say
  "accepted -- your teacher will review this one". A mark wrongly given can be taken back in
  the live feed; a mark wrongly refused is argued about in front of the whole class.

AUTO-MARKING -- what the file may mark, and what it must not:
  Multiple choice, true/false, fill in the blank, numeric, ordering/matching and short answer all
  have answer keys. Mark those the moment the student submits: 1 for correct, 0 for incorrect, and
  say plainly which it was. A true/false justification box is not marked; the mark is for the choice.
  OPEN-ENDED QUESTIONS ARE NEVER AUTO-MARKED. No keyword test can judge written reasoning -- a
  correct answer in words the pattern did not expect would score zero, which is worse than no score
  at all. On submit show "Submitted. Your teacher will mark this one.": no mark, no zero, and never
  the words wrong, incorrect or missing. Report it to LISM with mark null and maxMark 1, so it
  reaches the teacher marked as waiting.
  Never award a mark for length, for key terms alone, or for the box being non-empty.

Each question: ONE attempt. On submit, disable that input and its button, then give specific feedback
and reveal the correct answer for the auto-marked types. Wrong options are plausible misconceptions,
never filler.

RESULTS SECTION -- the student sees their own result at the end:
  · the auto-marked score as "X / Y", where Y counts only the auto-marked questions
  · every question listed right or wrong, with one sentence per wrong answer naming the idea to
    revisit
  · where the quiz has open-ended questions, a separate line "N marks are with your teacher",
    never counted as 0 and never folded into the score
  · no single percentage that treats unmarked work as lost marks
  If every question is auto-marked, the score IS the final mark and may be shown as such.


LISM LESSON INTEGRATION (required — do not remove this section)

Without this the uploaded file has no manifest, so LISM cannot show the activity's sections or let the
teacher pace them, and the whole activity collapses into one unnamed block.

A. Emit exactly one inert manifest in <head>:

The stages follow the pacing you chose. For [ONE SECTION], the stages array holds exactly ONE
stage: id "main-activity", label "Quiz", marks = total questions, autoMarks = the number of
auto-marked questions. For [PER QUESTION], one stage per question, q1 upward, as shown below.

<script type="application/json" id="lism-manifest">
{
  "lessonType": "quiz",
  "subject": "[SUBJECT]", "grade": "[GRADE]", "week": "[WEEK X]", "topic": "[TOPIC NAME]",
  "keywords": ["...", "..."],
  "deliveryMode": "lesson", "sessionType": "lesson",
  "stages": [
    {"id":"q1","label":"Question 1","type":"activity","durationSeconds":90,"sequentialLock":true,"marks":1,"autoMarks":1,"rubric":[{"label":"Accuracy","marks":1,"objective":true,"descriptor":"[write what a correct response contains for this topic]"}]},
    {"id":"q2","label":"Question 2","type":"activity","durationSeconds":90,"sequentialLock":true,"marks":1,"autoMarks":1,"rubric":[{"label":"Accuracy","marks":1,"objective":true,"descriptor":"[write what a correct response contains for this topic]"}]},
    {"id":"q3","label":"Question 3","type":"activity","durationSeconds":90,"sequentialLock":true,"marks":1,"autoMarks":1,"rubric":[{"label":"Accuracy","marks":1,"objective":true,"descriptor":"[write what a correct response contains for this topic]"}]},
    {"id":"q4","label":"Question 4","type":"activity","durationSeconds":90,"sequentialLock":true,"marks":1,"autoMarks":1,"rubric":[{"label":"Accuracy","marks":1,"objective":true,"descriptor":"[write what a correct response contains for this topic]"}]},
    {"id":"q5","label":"Question 5","type":"activity","durationSeconds":90,"sequentialLock":true,"marks":1,"autoMarks":1,"rubric":[{"label":"Accuracy","marks":1,"objective":true,"descriptor":"[write what a correct response contains for this topic]"}]},
    {"id":"q6","label":"Question 6","type":"activity","durationSeconds":90,"sequentialLock":true,"marks":1,"autoMarks":1,"rubric":[{"label":"Accuracy","marks":1,"objective":true,"descriptor":"[write what a correct response contains for this topic]"}]},
    {"id":"q7","label":"Question 7","type":"activity","durationSeconds":90,"sequentialLock":true,"marks":1,"autoMarks":1,"rubric":[{"label":"Accuracy","marks":1,"objective":true,"descriptor":"[write what a correct response contains for this topic]"}]},
    {"id":"q8","label":"Question 8","type":"activity","durationSeconds":90,"sequentialLock":true,"marks":1,"autoMarks":1,"rubric":[{"label":"Accuracy","marks":1,"objective":true,"descriptor":"[write what a correct response contains for this topic]"}]},
    {"id":"results","label":"Results","type":"activity","durationSeconds":120,"sequentialLock":true,"marks":null}
  ]
}
</script>

Use exactly these stage ids and order. Give the matching section in the HTML a data-stage="<id>"
attribute. If your activity genuinely needs more or fewer sections (for example a different number of
cards or puzzles), keep the same naming pattern and list every one of them in the manifest.

B. Detect LISM with `window.parent !== window`. When it is absent, run normally and self-paced.
   When present, hand pacing to the teacher and listen for
   {type:'lism:command', command, ...}:
     start_stage (stage) -> show ONLY that section, enable its inputs
     stage_ended         -> show "Waiting for your teacher…", disable that section
     pause / resume      -> freeze / unfreeze, never losing typed work
     lock (reason) / unlock
     set_config (copyPasteProtection, focusMonitoring, maxWarnings)
        -> when copyPasteProtection is on, block copy, cut, paste and right-click INSIDE ANSWER
           BOXES ONLY, never page-wide
     time_update (elapsedSeconds) -> store it; never render your own countdown, LISM shows the timer
   Ignore any unrecognised command silently. Never throw.

C. STUDENTS MUST SEE ONLY THE SECTION THE TEACHER HAS STARTED. Hide every other section until its
   start_stage arrives. Never auto-advance, and never reveal a later section early.

TURN THE STUDENT'S OWN NAVIGATION OFF INSIDE LISM. Next/Previous buttons, arrow keys, clickable
progress dots and swipe gestures exist ONLY for the file opened on its own. The moment you detect
LISM (window.parent !== window and not ?preview=1), REMOVE them from the page -- hidden, not merely
disabled. Nothing inside the activity may change which section is showing; only a start_stage
command from the teacher does that. ?preview=1 is the exception and keeps full navigation.

KEEP THE BOTTOM-RIGHT CORNER CLEAR -- LISM overlays its own student controls there. Do not place
buttons or floating elements in the bottom 90px of the viewport when LISM is present.


D. NO TIMERS OF YOUR OWN. No countdowns, clocks, progress rings, or time-based locking or advancing.
   The recommended duration is metadata in the manifest and may be printed as text beside a section
   title. LISM shows the one synchronised countdown.

E. Report every accepted answer to the teacher's live dashboard:
   window.parent.postMessage({type:'lism:event', event:'student_submitted',
     stageId, questionId, correct, answer, mark, maxMark, keywordsUsed}, '*')
   ALWAYS include `answer` with the student's actual text or choice — the teacher reads it aloud for
   feedback, and sending only `correct` leaves them nothing to discuss.
   Also emit activity_ready on load, stage_viewed when a section is shown, and activity_completed at
   the end with the final score.

F. Teacher preview: when the URL contains ?preview=1, show Previous/Next and a section counter, allow
   every section to be viewed in any order, and emit NO events at all.

G. Nothing is uploaded anywhere. Answers travel only via postMessage to the LISM page that embeds the
   activity. Opened standalone, nothing leaves the device at all.


MARKS

The manifest above already carries this activity's marks. Do not change the totals — LISM adds them up,
shows them to students, and puts them in the teacher's gradebook.

Each marked section declares three things:
  "marks"     what the section is worth in total
  "autoMarks" the part this activity can score by itself
  "rubric"    what each part is awarded for

Fill in every [square-bracket] descriptor in the rubric with wording specific to [TOPIC NAME] at Grade
[GRADE]. "Explains clearly" is not a descriptor — say what a correct explanation of THIS content must
contain.

WHAT YOU MAY SCORE AUTOMATICALLY: a chosen option, a matched pair, a placed item, a completed word, a
numeric answer, a passing test. Report these with `correct` and, when you know it, a numeric `mark` in
the student_submitted payload — never more than that section's autoMarks.

WHAT YOU MUST NOT SCORE: anything written in the student's own words — explanations, justifications,
reasoning, reflections, worked method. Send the text and leave `correct` as null. The teacher awards
those marks after reading the work, usually after discussing it with the class.

NEVER base `correct` on any of these — they are the ways auto-marking goes wrong:
  · whether the answer contains the lesson's keywords
  · word count, length, or "they wrote something"
  · similarity or fuzzy matching against a model answer
  · the student ticking their own self-assessment
A student who explains the idea perfectly in her own words would be marked wrong, and one who sprinkles
in the right nouns would be marked right. If you cannot name the single correct answer, it is not yours
to mark.

TELL THE STUDENT: on any marked section show "N marks — your teacher will see and review this". Do NOT
promise a grade or display a final total: the teacher decides whether and how to mark it, and a student
promised a number that never arrives stops believing the message.


EDUCATIONAL QUALITY (mandatory — this decides whether the activity is usable in a real classroom)

Every question must be about THIS topic's actual content. If a question would still make sense with a
different topic swapped in, it is not specific enough — rewrite it.

BANNED, because they assess nothing:
- "What do you remember about X?" / "What do you already know?"
- Options written as descriptions ("a correct statement", "a distractor", "a common misconception")
- "True or false: X always works the same way"
- Restating the topic name as if it were an answer

REQUIRED — choose the verb that fits the content: analyse this diagram/data · predict what happens
when… · explain why… · identify the error · classify these · interpret this graph · compare these two
cases · complete the missing step · label the diagram · order these steps · correct this misconception
· justify which explanation is better.

Every wrong option must be a real mistake a student at this grade actually makes — a specific wrong
value, a confused term, a step in the wrong order. If you cannot write plausible wrong answers for this
content, use an open response instead. Pitch the vocabulary, numbers and context to Grade [GRADE].

FEEDBACK: every response gets specific feedback that says how to improve, never just "wrong". Show the
correct answer only after the student has attempted it.


SELF-CHECK BEFORE YOU FINISH

State briefly that you have verified: the manifest is present, valid JSON, and lists every section with
matching data-stage attributes; only the teacher-started section is visible inside LISM and everything
works self-paced outside it; no timers or countdowns of your own anywhere; student_submitted is emitted
on every accepted answer and always carries `answer`; ?preview=1 gives Previous/Next and emits nothing;
the layout mirrors correctly with dir:'rtl' and Arabic answers are normalised before comparison; nothing
overlaps at 1366×768 or on a 390px phone; every question is specific to [TOPIC NAME] with plausible
wrong answers; and the file opens and works by double-clicking it offline.
